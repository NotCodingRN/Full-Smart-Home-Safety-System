# AWS IoT Certificates

Certificates are intentionally not included.

Create an AWS IoT Thing/certificate and place these files on the Pi:

- `AmazonRootCA1.pem`
- `device-certificate.pem.crt`
- `private.pem.key`

Never commit real `.key`, `.crt`, `.pem`, or AWS access key files.
