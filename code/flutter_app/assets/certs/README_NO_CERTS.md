# Mobile App AWS Certificates

Real AWS IoT certificates are not included. Replace the placeholder files only for a classroom demo build.

For production, do not ship private keys in an APK. Use Cognito, a custom authorizer, or a backend token service.
